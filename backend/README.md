# Backend

**Status:** Scaffold only — implementation not started.

A directory layout has been prepared for the Spring Boot backend:

```text
backend/src/main/java/com/supportticket/
backend/src/main/resources/db/migration/
```

Implementation begins with **Phase 1 — Backend Project Bootstrap** in `spec/implementation-plan.md`.

Do not add application code until following the approved specifications in `spec/`.
