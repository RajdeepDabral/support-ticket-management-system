# Backend API base URL (no trailing slash required)
# Host development: point at the published backend port
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Docker Compose production build uses relative /api/v1 (proxied by nginx).
